# ── Secrets / Environment ─────────────────────────────
.env
.env.*
!.env.example
!.env.deploy.example
*.pem
*.key

# 서버 배포 시 호스트에 생성되는 TLS 인증서/챌린지 (커밋 금지)
deploy/certbot/
secrets/

# ── Python (backend) ──────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
dist/
build/

# ── Node / React (frontend) ───────────────────────────
node_modules/
frontend/dist/
frontend/build/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Docker / Runtime data ─────────────────────────────
# 볼륨을 bind mount로 쓸 경우의 로컬 데이터 디렉터리
data/
minio_data/
pg_data/
redis_data/
*.log
logs/

# ── 백업 산출물 (scripts/backup.sh) ───────────────────
backups/

# ── IDE / Editor ──────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── OS ────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Claude Code / 로컬 도구 설정 (커밋 금지) ──────────
.claude/
.mcp.json
.playwright-mcp/

# 로컬 전용 접속 정보 (커밋 금지)
env
