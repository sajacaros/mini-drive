# Mini Drive backend 런타임 의존성 (pip 호환)
# pyproject.toml 과 동기화 유지. 상세 스펙은 pyproject.toml 참조.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# 통합 테스트(tests/integration_*)가 컨테이너 안에서 AsyncClient 로 사용 — 명시 의존.
# (이전에는 LLM 패키지의 전이 의존성으로 딸려 왔음)
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
python-multipart>=0.0.12
minio>=7.2.10
pillow>=11.0.0
pyjwt>=2.10.0
argon2-cffi>=23.1.0
# redis 는 권한 캐시·rate limit·다운로드 티켓에 쓰인다(LLM 무관 — 유지).
redis>=5.2.0
structlog>=24.4.0
prometheus-client>=0.21.0
